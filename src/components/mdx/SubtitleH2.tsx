import styled from 'styled-components';
import { textColor } from '../../styles/common';

export default styled.h2`
    ${textColor.title};
    display: block;
    text-align: left;
    font-size: 28px;
    margin-top: 30px;
    margin-bottom: 20px;
    font-weight: 400;
`;
